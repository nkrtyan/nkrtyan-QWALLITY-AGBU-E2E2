numbers = [15, 3, 28, 7, 10] 
print (numbers)
maximum = numbers[0]
for num in numbers:
    if num > maximum:
        maximum = num 
print(maximum)
minimum = numbers[0]
for num in numbers:
    if num < minimum:
        minimum = num
print(minimum)
total = 0
for num in numbers:
    total = total + num
print(total)  
 




 